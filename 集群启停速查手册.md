# 集群组件启停速查手册

> 所有命令均可从 **hadoop100** 执行（hadoop 用户 SSH 全互信）

---

## 一、Hadoop 生态

### Zookeeper（hadoop100/101/102）

```bash
ssh hadoop100 "zk.sh start"
ssh hadoop100 "zk.sh stop"
ssh hadoop100 "zk.sh status"
```

### Hadoop HDFS（hadoop100）

```bash
ssh hadoop100 "start-dfs.sh"
ssh hadoop100 "stop-dfs.sh"
```

### Hadoop YARN（hadoop101）

```bash
ssh hadoop101 "start-yarn.sh"
ssh hadoop101 "stop-yarn.sh"
```

### 历史服务器（hadoop100）

```bash
ssh hadoop100 "mapred --daemon start historyserver"
ssh hadoop100 "mapred --daemon stop historyserver"
```

### Kafka（hadoop100/101/102）

```bash
ssh hadoop100 "kf.sh start"
ssh hadoop100 "kf.sh stop"
```

---

## 二、数据采集

### 路径一：行为日志 → HDFS

> 日志文件 → Flume f1(TaildirSrc+KafkaChannel) → Kafka(topic_log) → Flume f2(Kafka+HDFS Sink) → HDFS /origin_data/gmall/log/

| 步骤 | 组件 | 节点 | 启动命令 |
|---|---|---|---|
| 1 | Zookeeper | 全节点 | `ssh hadoop100 "zk.sh start"` |
| 2 | Kafka | 全节点 | `ssh hadoop100 "kf.sh start"` |
| 3 | Flume f1（采集） | hadoop100/101 | `ssh hadoop100 "f1.sh start"` |
| 4 | Flume f2（消费落盘） | hadoop102 | `ssh hadoop102 "f2.sh start"` |

停止（逆序）：

```bash
ssh hadoop102 "f2.sh stop"
ssh hadoop100 "f1.sh stop"
ssh hadoop100 "kf.sh stop"
ssh hadoop100 "zk.sh stop"
```

### 路径二：业务数据 → HDFS

> MySQL binlog → Maxwell(CDC解析) → Kafka(topic_db) → Flume f3(Kafka+HDFS Sink) → HDFS /origin_data/gmall/db/

| 步骤 | 组件 | 节点 | 启动命令 |
|---|---|---|---|
| 1 | Zookeeper | 全节点 | `ssh hadoop100 "zk.sh start"` |
| 2 | Kafka | 全节点 | `ssh hadoop100 "kf.sh start"` |
| 3 | MySQL | hadoop100 | `ssh hadoop100 "sudo systemctl start mysqld"` |
| 4 | Maxwell | hadoop100 | `ssh hadoop100 "mxw.sh start"` |
| 5 | Flume f3（消费落盘） | hadoop102 | `ssh hadoop102 "f3.sh start"` |

停止（逆序）：

```bash
ssh hadoop102 "f3.sh stop"
ssh hadoop100 "mxw.sh stop"
ssh hadoop100 "kf.sh stop"
ssh hadoop100 "zk.sh stop"
```

### 两条路径一起启动

```bash
# ZK + Kafka（公共依赖，启一次）
ssh hadoop100 "zk.sh start"
ssh hadoop100 "kf.sh start"

# 路径一：日志
ssh hadoop100 "f1.sh start"
ssh hadoop102 "f2.sh start"

# 路径二：业务
ssh hadoop100 "sudo systemctl start mysqld"
ssh hadoop100 "mxw.sh start"
ssh hadoop102 "f3.sh start"
```

### 两条路径一起停止

```bash
# 路径二
ssh hadoop102 "f3.sh stop"
ssh hadoop100 "mxw.sh stop"

# 路径一
ssh hadoop102 "f2.sh stop"
ssh hadoop100 "f1.sh stop"

# 公共依赖
ssh hadoop100 "kf.sh stop"
ssh hadoop100 "zk.sh stop"
```

---

## 三、数据仓库

### Hive MetaStore + HiveServer2（hadoop100）

```bash
ssh hadoop100 "nohup hive --service metastore &"
ssh hadoop100 "nohup hive --service hiveserver2 &"

# 停止
ssh hadoop100 "ps -ef | grep -E 'metastore|hiveserver2' | grep -v grep | awk '{print \$2}' | xargs kill"
```

---

## 四、OLAP + 调度（hadoop102）+ 可视化（hadoop101）

### Doris FE

```bash
ssh hadoop102 "/opt/module/doris/fe/bin/start_fe.sh --daemon"
ssh hadoop102 "/opt/module/doris/fe/bin/stop_fe.sh"

# 验证
ssh hadoop102 "curl http://hadoop102:8030/api/bootstrap"
```

### Doris BE

```bash
ssh hadoop102 "ulimit -n 65536 && /opt/module/doris/be/bin/start_be.sh --daemon"
ssh hadoop102 "/opt/module/doris/be/bin/stop_be.sh"
```

### DolphinScheduler

```bash
ssh hadoop102 "/opt/module/dolphinscheduler/bin/dolphinscheduler-daemon.sh start standalone-server"
ssh hadoop102 "/opt/module/dolphinscheduler/bin/dolphinscheduler-daemon.sh stop standalone-server"
ssh hadoop102 "/opt/module/dolphinscheduler/bin/dolphinscheduler-daemon.sh status standalone-server"
```

### Superset（hadoop101）

```bash
ssh hadoop101 "superset.sh start"
ssh hadoop101 "superset.sh stop"
ssh hadoop101 "superset.sh restart"
ssh hadoop101 "superset.sh status"
```

---

## 五、一键全链路

```bash
# ===== 启动（按依赖顺序）=====
# 1. Hadoop + ZK + Kafka
ssh hadoop100 "zk.sh start"
ssh hadoop100 "start-dfs.sh"
ssh hadoop101 "start-yarn.sh"
ssh hadoop100 "mapred --daemon start historyserver"
ssh hadoop100 "kf.sh start"

# 2. 路径一：行为日志
ssh hadoop100 "f1.sh start"
ssh hadoop102 "f2.sh start"

# 3. 路径二：业务数据
ssh hadoop100 "mxw.sh start"
ssh hadoop102 "f3.sh start"

# ===== 停止（逆序）=====
# 3. 路径二
ssh hadoop102 "f3.sh stop"
ssh hadoop100 "mxw.sh stop"

# 2. 路径一
ssh hadoop102 "f2.sh stop"
ssh hadoop100 "f1.sh stop"

# 1. Hadoop + ZK + Kafka
ssh hadoop100 "kf.sh stop"
ssh hadoop100 "mapred --daemon stop historyserver"
ssh hadoop101 "stop-yarn.sh"
ssh hadoop100 "stop-dfs.sh"
ssh hadoop100 "zk.sh stop"
```

---

## 六、Web 控制台

| 组件 | 地址 | 账号 / 密码 |
|---|---|---|
| HDFS NameNode | http://hadoop100:9870 | — |
| YARN ResourceManager | http://hadoop101:8088 | — |
| Doris FE | http://hadoop102:8030 | root / root |
| DolphinScheduler | http://hadoop102:12345/dolphinscheduler | admin / dolphinscheduler123 |
| Superset | http://hadoop101:8787 | admin / admin123 |
