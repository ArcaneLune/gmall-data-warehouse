# 电商离线数据仓库 V6.0 — 项目介绍

## 1. 项目概述

本项目是一个完整的电商大数据离线数仓系统，项目从零搭建 3 节点虚拟机集群，覆盖数据采集、数据传输、数据仓库分层建模、OLAP 加速、任务调度、可视化报表的全流程，形成端到端的离线数仓解决方案。

**核心目标**：将电商平台的用户行为日志和业务数据库中的交易数据，经过采集、清洗、建模、汇总，最终产出面向业务的统计指标和可视化报表，支撑运营决策。

---

## 2. 技术架构

### 2.1 整体数据流

```
                    用户行为日志(Mock JAR)              业务数据(MySQL gmall)
                           │                                  │
                           │ Flume f1(TaildirSrc+             ├── Maxwell (CDC)
                           │  KafkaChannel)                   ├── DataX  (全量)
                           ▼                                  │
                      Kafka(topic_log)                        ▼
                           │                         Kafka(topic_db)
                           │                                  │
                           ├─── Flume f2(KafkaSrc+ ───────────┼─── Flume f3(KafkaSrc+
                           │     HDFS Sink)                   │     HDFS Sink)
                           ▼                                  ▼
                    HDFS /origin_data/gmall/log/     HDFS /origin_data/gmall/db/
                           │                                  │
                           └──────────────┬───────────────────┘
                                          │ Hive LOAD DATA
                                          ▼
            ┌──────────────────────────────────────────────────────┐
            │                  Hive 数仓分层 (Hive on Spark)        │
            │  ODS(30表) → DIM(9表) + DWD(10表)                    │
            │                    → DWS(15表) → ADS(16指标)          │
            │              存储: ORC+Snappy / ADS: \t文本           │
            └──────────────────────┬───────────────────────────────┘
                                   │ Broker Load (WITH HDFS)
                                   ▼
                          Doris 2.0.3 (OLAP 加速)
                                   │
                                   ▼
                          Superset 2.0.0 (可视化仪表盘)
                                   ▲
                                   │
                     DolphinScheduler 2.0.5 (全链路定时调度)
```

### 2.2 技术栈一览

| 层级 | 组件 | 版本 | 用途 |
|------|------|------|------|
| 操作系统 | CentOS | 7.9 | 三台虚拟机操作系统 |
| 开发语言 | JDK | 1.8.0_212 | Java运行环境，全局版本基准 |
| 数据生成 | gmall-remake-mock | 2023-05-15-3 | 模拟生成用户行为日志和业务数据 |
| 数据采集 | Flume | 1.10.1 | 日志采集→Kafka（TaildirSrc+KafkaChannel），Kafka消费→HDFS（KafkaSrc+HDFSSink） |
| 消息队列 | Kafka | 2.12-3.3.1 | 日志/业务数据缓冲通道（topic_log / topic_db） |
| CDC工具 | Maxwell | 1.29.2（定制版，支持mock_date） | MySQL binlog实时采集→Kafka |
| 离线同步 | DataX | 202309 | MySQL全量表→HDFS批量同步（17张全量表） |
| 分布式存储 | Hadoop | 3.3.6 | HDFS（NameNode/DataNode）+ YARN（ResourceManager/NodeManager） |
| 协调服务 | Zookeeper | 3.7.1 | 集群协调 + Kafka元数据管理 |
| 关系型数据库 | MySQL | 8.0.39 | Hive元数据(metastore) + 业务数据(gmall) + DolphinScheduler元数据 + Superset元数据 |
| 数仓引擎 | Hive | 3.1.3（重编译适配Spark 3.3.1） | SQL解析优化 + 元数据管理 |
| 计算引擎 | Spark | 3.3.1（纯净版，不含Hadoop） | 替代MR作为Hive执行引擎，YARN调度 |
| OLAP引擎 | Apache Doris | 2.0.3 | ADS 报表查询加速，BE 内置 HDFS 支持 |
| 任务调度 | DolphinScheduler | 2.0.5 | Standalone 模式部署，全链路 DAG 定时调度 |
| 数据可视化 | Superset | 2.0.0 | 仪表盘 + 图表展示，MySQL 协议直连 Doris |
| Python环境 | Miniconda + Python | 3.8.16 | Superset运行环境（conda虚拟环境） |
| Web服务器 | gunicorn | - | Superset生产部署（类比Tomcat） |

---

## 3. 集群规划

### 3.1 节点角色（3节点虚拟机）

| 服务 | hadoop100 (4GB) | hadoop101 (4GB) | hadoop102 (8GB) |
|------|:---:|:---:|:---:|
| NameNode | ✓ | | |
| DataNode | ✓ | ✓ | ✓ |
| SecondaryNameNode | | | ✓ |
| ResourceManager | | ✓ | |
| NodeManager | ✓ | ✓ | ✓ |
| Zookeeper | ✓ | ✓ | ✓ |
| Kafka | ✓ | ✓ | ✓ |
| Flume(日志采集 f1) | ✓ | ✓ | |
| Flume(日志消费 f2) | | | ✓ |
| Flume(业务消费 f3) | | | ✓ |
| Hive | ✓ | | |
| MySQL | ✓ | | |
| DataX | ✓ | | |
| Spark | ✓ | | |
| Maxwell | ✓ | | |
| Doris FE + BE | | | ✓ |
| DolphinScheduler | | | ✓ (Standalone) |
| Superset | | ✓ | |

### 3.2 关键路径

| 路径 | 说明 |
|------|------|
| `/opt/module/` | 各组件安装目录 |
| `/opt/module/applog/` | 日志模拟器(jar+配置+日志输出) |
| `/home/hadoop/bin/` | 全局启停脚本 |
| `/origin_data/gmall/` | HDFS原始数据 |
| `/warehouse/gmall/` | Hive数仓数据 |

---

## 4. 业务数据模型

### 4.1 业务表概览（MySQL gmall 库，36张表）

**核心业务域**：

- **商品域**：sku_info, spu_info, base_trademark, base_category1/2/3, sku_attr_value, sku_sale_attr_value
- **交易域**：order_info, order_detail, order_detail_activity, order_detail_coupon, order_status_log, order_refund_info
- **支付域**：payment_info, refund_payment
- **营销域**：activity_info, activity_rule, coupon_info, coupon_range, coupon_use, promotion_pos, promotion_refer
- **用户域**：user_info, user_address
- **公共域**：base_province, base_region, base_dic, base_attr_info, base_attr_value
- **互动域**：cart_info, favor_info, comment_info

### 4.2 同步策略

| 策略 | 表数 | 典型表 | 工具 |
|------|:---:|-------|------|
| 全量同步(每日) | 17 | activity_info, base_category1/2/3, sku_info, spu_info, base_trademark... | DataX |
| 增量同步(每日) | 13 | order_info, order_detail, payment_info, user_info, cart_info... | Maxwell CDC + Flume |

---

## 5. 数仓分层设计

### 5.1 分层架构

```
ADS  (应用层)  — 16张报表，\t 文本格式，面向 Doris/Superset
DWS  (汇总层)  — 15张汇总表，1日(n=9) / n日(n=2) / 历史至今(n=2)，ORC+Snappy
DWD  (明细层)  — 10张事实表，事务型/周期快照/累积快照，ORC+Snappy
DIM  (维度层)  — 9张维度表，星型模型，ORC+Snappy
ODS  (贴源层)  — 30张表，与源数据保持1:1映射
```

### 5.2 各层详情

#### ODS 层（30张表）

- 1 张日志表：`ods_log_inc`（JSON格式，Hive STRUCT/ARRAY类型映射）
- 17 张全量表：`ods_{table}_full`（\t 分隔文本，gzip压缩）
- 12 张增量表：`ods_{table}_inc`（Maxwell JSON格式，含 type/ts/data/old 字段）

#### DIM 层（9张维度表，ORC + Snappy）

| 维度表 | 主维表 | 关联表数 |
|-------|--------|:------:|
| dim_sku_full（商品） | sku_info | 8张 |
| dim_coupon_full（优惠券） | coupon_info | 2张 |
| dim_activity_full（活动） | activity_info | 2张 |
| dim_province_full（地区） | base_province | 2张 |
| dim_promotion_pos_full（营销坑位） | promotion_pos | 1张 |
| dim_promotion_refer_full（营销渠道） | promotion_refer | 1张 |
| dim_date（日期） | - | 静态生成 |
| dim_user_zip（用户拉链表） | user_info | 1张 |

#### DWD 层（10张事实表）

| 事实表 | 类型 | 数据域 | 粒度 |
|-------|------|-------|------|
| dwd_trade_cart_add_inc | 事务型 | 交易域 | 一次加购 |
| dwd_trade_order_detail_inc | 事务型 | 交易域 | 订单-商品 |
| dwd_trade_cart_full | 周期快照 | 交易域 | 每日-购物车 |
| dwd_trade_trade_flow_acc | 累积快照 | 交易域 | 订单级(下单/支付/确认) |
| dwd_trade_pay_detail_suc_inc | 事务型 | 交易域 | 一次支付 |
| dwd_tool_coupon_used_inc | 事务型 | 工具域 | 一次使用 |
| dwd_interaction_favor_add_inc | 事务型 | 互动域 | 一次收藏 |
| dwd_user_register_inc | 事务型 | 用户域 | 一个用户 |
| dwd_user_login_inc | 事务型 | 用户域 | 一次登录 |
| dwd_traffic_page_view_inc | 事务型 | 流量域 | 一次动作事件 |

#### DWS 层（15张汇总表）

- **最近1日汇总（9张）**：交易域(用户-商品/用户/省份粒度)、工具域、互动域、流量域(会话/访客页面)
- **最近n日汇总（2张）**：交易域(用户-商品/省份粒度)
- **历史至今汇总（2张）**：交易域用户订单累计、用户域登录累计

#### ADS 层（16张报表，\t 文本）

| 主题 | 报表 |
|------|------|
| 流量 | 各渠道流量统计、路径分析 |
| 用户 | 用户变动统计、留存率、新增活跃统计、漏斗分析、新增下单用户、7日内连续3日下单 |
| 商品 | 品牌复购率、品牌下单统计、品类下单统计、购物车存量Top3、收藏次数Top3 |
| 交易 | 下单到支付平均间隔、各省份交易统计 |
| 优惠券 | 优惠券使用统计 |

---

## 6. 核心脚本清单

| 脚本 | 位置 | 功能 |
|------|------|------|
| cluster.sh | /home/hadoop/bin/ | 集群整体启停 |
| xsync / xcall | /home/hadoop/bin/ | 集群文件分发 / 批量命令执行 |
| zk.sh / kf.sh / hdp.sh | /home/hadoop/bin/ | Zookeeper / Kafka / Hadoop 启停 |
| lg.sh | /home/hadoop/bin/ | 生成模拟日志数据 |
| f1.sh / f2.sh / f3.sh | /home/hadoop/bin/ | Flume 采集端 / 日志消费 / 业务消费启停 |
| mxw.sh | /home/hadoop/bin/ | Maxwell 启停 |
| mysql_to_hdfs_full.sh | /home/hadoop/bin/ | DataX 全量表同步 |
| hdfs_to_ods_log.sh | /home/hadoop/bin/ | ODS 日志表装载 |
| hdfs_to_ods_db.sh | /home/hadoop/bin/ | ODS 业务表装载 |
| ods_to_dim.sh | /home/hadoop/bin/ | DIM 层每日装载 |
| ods_to_dwd.sh | /home/hadoop/bin/ | DWD 层每日装载 |
| dwd_to_dws_1d.sh | /home/hadoop/bin/ | DWS 1日汇总表每日装载 |
| dws_1d_to_dws_nd.sh | /home/hadoop/bin/ | DWS n日汇总表 |
| dws_1d_to_dws_td.sh | /home/hadoop/bin/ | DWS 历史至今汇总表每日装载 |
| dws_to_ads.sh | /home/hadoop/bin/ | ADS 层装载 |
| ads_broker_load.sh | /home/hadoop/bin/ | ADS → Doris 数据同步 |
| superset.sh | /home/hadoop/bin/ | Superset 启停 |

---

## 7. 自定义代码

### 7.1 Flume拦截器（需Maven打包部署）

| 拦截器 | 用途 | 部署节点 |
|-------|------|:------:|
| TimestampInterceptor | 解决零点漂移：从日志JSON的ts字段取时间戳写header | hadoop102 |
| TimestampAndTableNameInterceptor | 提取Maxwell JSON的ts和table字段，实现HDFS按表名分目录 | hadoop102 |

### 7.2 Maxwell定制

- 源码增加 `mock_date` 参数，使教学环境下输出时间戳日期与业务日期一致
- 配置：`mock_date=2026-06-18`

---

## 8. 项目构建顺序

```
1. 基础设施层
   JDK → Hadoop → Zookeeper → 集群脚本(xsync/xcall) → SSH免密

2. 数据采集层
   Kafka → Flume(采集+消费) → MySQL → Maxwell → DataX

3. 数仓建模层
   Hive → Hive on Spark → ODS建表装载 → DIM → DWD → DWS → ADS

4. OLAP 加速层
   Doris 单机部署 → ADS 表建表 → Broker Load 导入

5. 调度与可视化层
   DolphinScheduler Standalone → 10脚本 DAG 工作流
   Superset 部署 → 连接 Doris → 16张报表可视化

6. 验证层
   模拟数据生成 → 全链路测试 → 报表可视化验证
```

---

## 9. 模拟数据说明

- **上线日期**：2026-06-18
- **历史数据**：2026-06-18 ~ 2026-06-21（4天）
- **首日操作**：全量表同步 + 增量表 Maxwell bootstrap 全量初始化
- **每日操作**：增量表 Maxwell CDC + 全量表 DataX 全量 + DolphinScheduler 定时调度

---

*版本：V6.0 | 集群规模：3节点虚拟机 | 计算引擎：Hive on Spark 3.3.1 | OLAP：Doris 2.0.3*
