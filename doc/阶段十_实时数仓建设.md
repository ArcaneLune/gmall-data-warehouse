# 阶段十：实时数仓建设

## 1. 数仓开发之 ODS 层

### 1.1 实时数仓 ODS 层

采集到 Kafka 的 topic_log 和 topic_db 主题的数据即为实时数仓的 ODS 层，这一层的作用是对数据做原样展示和备份。参见采集文档。

需要注意的是，离线数仓模拟数据的业务时间是打散在一天不同时刻的，而实时要求数据的业务时间为系统时间，修改 application.yml，将 mock.if-realtime 置为 1 即可。

在 hadoop100 的 /opt/module/applog 下 vim application.yml

修改参数：

```yaml
mock.if-realtime: 1
```

### 1.2 Kafka 数据有序

本项目要求 Flink 单个并行度的数据严格有序（主要用于数据去重），为了保证这一点，做了如下配置。

#### 1.2.1 修改 Kafka 分区数

修改 Kafka 配置文件 server.properties，将 topic 分区数设置为 4

```bash
[hadoop@hadoop100 ~]$ cd /opt/module/kafka/
[hadoop@hadoop100 kafka]$ vim config/server.properties
```

```properties
# 在配置文件中修改 Kafka 主题的默认分区数
num.partitions=4
```

三台服务器的配置文件都要修改，修改完毕重启 Kafka，不可直接分发配置文件，因为每个节点的 broker.id 必须唯一。分发后必须修改 broker.id 才能重启 Kafka，否则重启失败。

#### 1.2.2 Flink 程序并行度

在 Flink 程序中设置并行度为 4。

Flink 程序从 Kafka 消费数据时会启动同属于一个消费者组的四个消费者，Kafka 消费者的默认分区分配策略是 Range + CooperativeSticky，消费者数和分区数相同时，每个消费者消费一个分区的数据。只要单分区数据有序，即可保证 Flink 单个并行度数据有序。

在 Kafka 1.x 及之后的版本中，保证数据单分区有序，条件如下：

（1）未开启幂等性

max.in.flight.requests.per.connection 需要设置为 1。该参数指定了生产者在收到服务器响应之前可以发送多少个消息。

（2）开启幂等性

max.in.flight.requests.per.connection 需要设置小于等于 5。

原因说明：因为在 kafka 1.x 以后，启用幂等后，kafka 服务端会缓存 producer 发来的最近 5 个 request 的元数据，故无论如何，都可以保证最近 5 个 request 的数据都是有序的。

默认情况下幂等性是开启的，max.in.flight.requests.per.connection 默认值为 5，所以单分区数据默认是有序的，不需要做任何配置。

综上，我们可以保证 Flink 程序单个并行度的数据有序。

#### 1.2.3 数据生成与验证

1）在 hadoop100 执行 lg.sh test 100 生成日志数据到本地文件

```bash
[hadoop@hadoop100 ~]$ lg.sh test 100
```

2）启动 zookeeper

```bash
[hadoop@hadoop100 ~]$ zk.sh start
```

3）启动 kafka

```bash
[hadoop@hadoop100 ~]$ kf.sh start
```

4）启动一个命令行 Kafka 消费者，消费 topic_log 主题的数据

```bash
[hadoop@hadoop100 ~]$ /opt/module/kafka/bin/kafka-console-consumer.sh --bootstrap-server hadoop100:9092 --topic topic_log
```

5）再在 hadoop100 运行 f1.sh 脚本使 flume 采集日志文件

```bash
[hadoop@hadoop100 ~]$ f1.sh
```

6）再执行日志生成脚本 lg.sh

```bash
[hadoop@hadoop100 ~]$ lg.sh
```

7）观察命令行 Kafka 消费者是否消费到数据。

### 1.3 业务数据环境准备

#### 1.3.1 创建实时业务数据库

在 hadoop100 上的 mysql 建立实时业务数据库

- 数据库名：gmall2026
- 字符集：utf8mb4
- 排序集：utf8mb4_general_ci



```sql
CREATE DATABASE IF NOT EXISTS gmall2026 DEFAULT CHARACTER SET utf8mb4 DEFAULT COLLATE utf8mb4_general_ci;
```

导入建表数据 gmall.sql

#### 1.3.2 修改 MySQL 配置

修改 /etc/my.cnf 文件

```bash
[hadoop@hadoop100 module]$ sudo vim /etc/my.cnf
```

```ini
server-id=1
log-bin=mysql-bin
binlog_format=row
binlog-do-db=gmall2026
```

#### 1.3.3 重启 MySQL 使配置生效

```bash
[hadoop@hadoop100 module]$ sudo systemctl restart mysqld
```

#### 1.3.4 模拟生成数据

1）修改 /opt/module/applog/application.yml 中数据库连接信息

```yaml
jdbc:mysql://hadoop100:3306/gmall2026?characterEncoding=utf-8&allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=GMT%2B8
```

2）然后运行 lg.sh

```bash
[hadoop@hadoop100 applog]$ lg.sh
```

### 1.4 Maxwell 配置

#### 1.4.1 修改 Maxwell 配置文件

```bash
[hadoop@hadoop100 ~]$ vim /opt/module/maxwell/config.properties
```

```properties
# Maxwell 数据发送目的地：Kafka
producer=kafka

# Kafka 集群地址
kafka.bootstrap.servers=hadoop100:9092,hadoop101:9092,hadoop102:9092

# Kafka topic（业务数据统一发到 topic_db）
kafka_topic=topic_db

# ====== MySQL 连接配置 ======
host=hadoop100
user=maxwell
password=maxwell
jdbc_options=useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true

# 过滤 gmall2026.z_log 表（日志数据备份表，无须采集）
filter=exclude:gmall2026.z_log

# 需要添加 后续初始化会用
client_id=maxwell_1
```

#### 1.4.2 编写 Maxwell 启动脚本

在 /home/hadoop/bin 目录下编写 maxwell.sh 启动脚本

```bash
[hadoop@hadoop100 ~]$ vim /home/hadoop/bin/maxwell.sh
```

```bash
/opt/module/maxwell/bin/maxwell --config /opt/module/maxwell/config.properties >/dev/null 2>&1 &
```

授予执行权限

```bash
[hadoop@hadoop100 ~]$ sudo chmod +x /home/hadoop/bin/maxwell.sh
```

运行启动程序

```bash
[hadoop@hadoop100 ~]$ maxwell.sh
```

#### 1.4.3 验证 Maxwell 采集

启动 Kafka 消费客户端，观察结果

```bash
[hadoop@hadoop100 ~]$ /opt/module/kafka/bin/kafka-console-consumer.sh --bootstrap-server hadoop100:9092 --topic topic_db
```

执行 lg.sh 生成模拟数据

```bash
[hadoop@hadoop100 ~]$ lg.sh
```

### 1.5 同步维度历史数据

实时计算不考虑历史的事实数据，但要考虑历史维度数据。因此要对维度相关的业务表做一次全量同步。

#### 1.5.1 维度相关的业务表

与维度相关的业务表如下：

```
activity_info
activity_rule
activity_sku
base_category1
base_category2
base_category3
base_dic
base_province
base_region
base_trademark
coupon_info
coupon_range
financial_sku_cost
sku_info
spu_info
user_info
```

#### 1.5.2 创建同步脚本

切换到 /home/hadoop/bin 目录，创建 mysql_to_kafka_init.sh 文件

```bash
[hadoop@hadoop100 ~]$ cd ~/bin
[hadoop@hadoop100 bin]$ vim mysql_to_kafka_init.sh
```

在脚本中添加如下内容：

```bash
#!/bin/bash

# 该脚本的作用是初始化所有的业务数据，只需执行一次

MAXWELL_HOME=/opt/module/maxwell

import_data() {
    $MAXWELL_HOME/bin/maxwell-bootstrap --database gmall2026 --table $1 --config $MAXWELL_HOME/config.properties
}

case $1 in
"activity_info")
  import_data activity_info
  ;;
"activity_rule")
  import_data activity_rule
  ;;
"activity_sku")
  import_data activity_sku
  ;;
"base_category1")
  import_data base_category1
  ;;
"base_category2")
  import_data base_category2
  ;;
"base_category3")
  import_data base_category3
  ;;
"base_dic")
  import_data base_dic
  ;;
"base_province")
  import_data base_province
  ;;
"base_region")
  import_data base_region
  ;;
"base_trademark")
  import_data base_trademark
  ;;
"coupon_info")
  import_data coupon_info
  ;;
"coupon_range")
  import_data coupon_range
  ;;
"financial_sku_cost")
  import_data financial_sku_cost
  ;;
"sku_info")
  import_data sku_info
  ;;
"spu_info")
  import_data spu_info
  ;;
"user_info")
  import_data user_info
  ;;
"all")
  import_data activity_info
  import_data activity_rule
  import_data activity_sku
  import_data base_category1
  import_data base_category2
  import_data base_category3
  import_data base_dic
  import_data base_province
  import_data base_region
  import_data base_trademark
  import_data coupon_info
  import_data coupon_range
  import_data financial_sku_cost
  import_data sku_info
  import_data spu_info
  import_data user_info
  ;;
esac
```

#### 1.5.3 执行同步脚本

1）增加执行权限

```bash
[hadoop@hadoop100 bin]$ chmod +x mysql_to_kafka_init.sh
```

2）执行脚本（注意：需要启动 maxwell 本身进程）

```bash
[hadoop@hadoop100 bin]$ mysql_to_kafka_init.sh all
```

3）启动 Kafka 消费者，观察数据是否写入 Kafka

```bash
[hadoop@hadoop100 bin]$ kafka-console-consumer.sh --bootstrap-server hadoop100:9092 --topic topic_db
```

## 2. 数仓开发之 DIM 层

### 2.1 DIM 层设计要点

1）DIM 层的设计依据是维度建模理论，该层存储维度模型的维度表。

2）DIM 层的数据存储在 HBase 表中

DIM 层表是用于维度关联的，要通过主键去获取相关维度信息，这种场景下 K-V 类型数据库的效率较高。常见的 K-V 类型数据库有 Redis、HBase，而 Redis 的数据常驻内存，会给内存造成较大压力，因而选用 HBase 存储维度数据。

3）DIM 层表名的命名规范为 dim_表名

### 2.2 配置表

#### 2.2.1 配置表设计

1）字段解析

我们将为维度分流准备配置表 table_process_dim，为该表设计五个字段，如下：

（1）source_table：作为数据源的业务数据表名

（2）sink_table：作为数据目的地的 HBase 表名

（3）sink_family：作为数据目的地的 HBase 列族

（4）sink_columns：写入 HBase 的字段

（5）sink_row_key：写入 HBase 需要指定的 row_key

将 sink_table 作为配置表的主键，可以通过它获取唯一的配置信息，明确维度分流的数据源及目的地等信息。

#### 2.2.2 创建配置数据库和表

1）创建数据库 gmall2026_config，注意：和 gmall2026 业务库区分开

```bash
[hadoop@hadoop100 dblog]$ mysql -uroot -proot -e"create database gmall2026_config charset utf8mb4 default collate utf8mb4_general_ci"
```

2）执行建表语句以及数据导入 SQL 文件

```
dim维度初始配置.sql
```

#### 2.2.3 修改 MySQL 配置

在 MySQL 配置文件中增加 gmall2026_config 开启 Binlog

```bash
[hadoop@hadoop100 ~]$ sudo vim /etc/my.cnf
```

增加对于 gmall2026_config 的监控

```ini
#数据库id
server-id = 1
#启动binlog，该参数的值会作为binlog的文件名
log-bin=mysql-bin
#binlog类型，maxwell要求为row类型
binlog_format=row
#启用binlog的数据库，需根据实际情况作出修改
binlog-do-db=gmall2026
binlog-do-db=gmall2026_config
```

保存退出。

#### 2.2.4 重启 MySQL 服务

```bash
[hadoop@hadoop100 ~]$ sudo systemctl restart mysqld
```

### 2.3 主要任务

#### 2.3.1 接收 Kafka 数据，过滤空值数据

对 Maxwell 抓取的数据进行 ETL，有用的部分保留，没用的过滤掉。

#### 2.3.2 动态拆分维度表功能

实时数仓的维度表用于补充事实数据的维度信息（维度关联），事实数据是以流的形式存在的，如果维度信息也是这样，维度关联就要通过双流 join 来实现，而通常维度数据的生成时间可能远远早于事实数据，要保证二者可以正确关联，就要把维度流的数据缓存下来，设置一个极大的 ttl，使其常驻内存。对于数据量比较小的维度而言，这样做没有什么问题，但如果是用户这样数据量在百 G 以上的维度，必然会带来极大的内存开销，不是一个很好的解决方案。

通常，我们会将维度数据写入外部存储介质，通常是 HBase，Redis 或 MySQL 这样的 K-V 数据库，以便通过主键高效地获取维度信息。本项目选用 HBase，主要是考虑到大数据场景下某些维度表数据量极大，Redis 和 MySQL 可能扛不住这样的压力。HBase 的设计初衷就是支持海量数据的读写操作，可以托管数十亿行、数百万列的大表，并且支持随机查询，可以通过 row_key 快速定位目标数据。

所有维度都存在 Kafka 的 topic_db 主题，不同的维度表上下游、字段信息不同，可以把这些信息记录在配置文件中，在程序启动时加载，根据配置信息完成维度数据的处理。这种方案有一个非常严重的问题，程序启动后并不能动态感知配置变化，当业务端需求变化时必须重启计算程序读取更新后的配置文件，效率极低。此处需要一种动态配置方案，当配置变化时，流处理程序可以自动感知。此处提出配置方案：用 mysql 数据库存储，通过 FlinkCDC 监控配置表数据变更。

该方案可以实现变更数据的实时监控，且配置信息存储在 MySQL，符合开发习惯，易于维护，配置变更操作简便。

综上，此处选择该方案。通过 FlinkCDC 读取配置表生成配置流，将其作为广播流与主流关联，将配置变更实时传达给主流。

#### 2.3.3 把流中的数据保存到对应的维度表

维度数据保存到 HBase 的表中。

### 2.4 项目工程搭建

#### 2.4.1 Git 分支管理

在 GitHub 的 gmall2026-realtime 仓库下新建分支 dev。

在 IDEA 中的 gmall2026-realtime 项目中创建本地分支 dev，并追踪远端仓库 dev 分支。在命令行切换至 dev 后，IDEA 已同步更新。

分支使用规范：

- 开发都在本地的 dev 分支完成，每完成一个模块的开发就提交至远端 dev 分支
- 管理员会将 dev 分支合并至某个 release 分支和 master 分支
- 本地的 master 分支只需要和远端的 master 分支保持一致即可，不会将本地 master 的内容提交至远端 master 分支（通常没有权限）

> DIM层、DWD层、DWS层详细代码与结构请参考项目代码仓库gmall2026-realtime

> 以下为DWS层Doris的建表语句

```sql
-- 12.1 流量域搜索关键词粒度页面浏览各窗口汇总表
drop table if exists gmall2026_realtime.dws_traffic_source_keyword_page_view_window;
create table if not exists gmall2026_realtime.dws_traffic_source_keyword_page_view_window
(
    `stt`           DATETIME COMMENT '窗口起始时间',
    `edt`           DATETIME COMMENT '窗口结束时间',
    `cur_date`      DATE COMMENT '当天日期',
    `keyword`       VARCHAR(128) COMMENT '搜索关键词',
    `keyword_count` BIGINT REPLACE COMMENT '搜索关键词出现次数'
) engine = olap aggregate key (`stt`,`edt`,`cur_date`,`keyword`)
partition by range(`cur_date`)()
distributed by hash(`stt`) buckets 10
properties (
"replication_num" = "3",
"dynamic_partition.enable" = "true",
"dynamic_partition.time_unit" = "DAY",
"dynamic_partition.end" = "3",
"dynamic_partition.prefix" = "par",
"dynamic_partition.buckets" = "10"
);

-- 12.2 流量域版本-渠道-地区-访客类别粒度页面浏览各窗口汇总表
drop table if exists gmall2026_realtime.dws_traffic_vc_ch_ar_is_new_page_view_window;
create table if not exists gmall2026_realtime.dws_traffic_vc_ch_ar_is_new_page_view_window
(
    `stt`      DATETIME COMMENT '窗口起始时间',
    `edt`      DATETIME COMMENT '窗口结束时间',
    `cur_date` DATE COMMENT '当天日期',
    `vc`       VARCHAR(256) COMMENT '版本号',
    `ch`       VARCHAR(256) COMMENT '渠道',
    `ar`       TINYINT COMMENT '地区',
    `is_new`   TINYINT COMMENT '新老访客状态标记',
    `uv_ct`    BIGINT REPLACE COMMENT '独立访客数',
    `sv_ct`    BIGINT REPLACE COMMENT '会话数',
    `pv_ct`    BIGINT REPLACE COMMENT '页面浏览数',
    `dur_sum`  BIGINT REPLACE COMMENT '累计访问时长'
) engine = olap aggregate key (`stt`,`edt`,`cur_date`,`vc`,`ch`,`ar`,`is_new`)
partition by range(`cur_date`)()
distributed by hash(`stt`) buckets 10
properties (
"replication_num" = "3",
"dynamic_partition.enable" = "true",
"dynamic_partition.time_unit" = "DAY",
"dynamic_partition.end" = "3",
"dynamic_partition.prefix" = "par",
"dynamic_partition.buckets" = "10"
);

-- 12.3 流量域首页、详情页页面浏览各窗口汇总表
drop table if exists gmall2026_realtime.dws_traffic_home_detail_page_view_window;
create table if not exists gmall2026_realtime.dws_traffic_home_detail_page_view_window
(
    `stt`               DATETIME COMMENT '窗口起始时间',
    `edt`               DATETIME COMMENT '窗口结束时间',
    `cur_date`          DATE COMMENT '当天日期',
    `home_uv_ct`        BIGINT REPLACE COMMENT '首页独立访客数',
    `good_detail_uv_ct` BIGINT REPLACE COMMENT '商品详情页独立访客数'
) engine = olap aggregate key (`stt`,`edt`,`cur_date`)
partition by range(`cur_date`)()
distributed by hash(`stt`) buckets 10
properties (
"replication_num" = "3",
"dynamic_partition.enable" = "true",
"dynamic_partition.time_unit" = "DAY",
"dynamic_partition.end" = "3",
"dynamic_partition.prefix" = "par",
"dynamic_partition.buckets" = "10"
);

-- 12.4 用户域用户登录各窗口汇总表
drop table if exists gmall2026_realtime.dws_user_user_login_window;
create table if not exists gmall2026_realtime.dws_user_user_login_window
(
    `stt`      DATETIME COMMENT '窗口起始时间',
    `edt`      DATETIME COMMENT '窗口结束时间',
    `cur_date` DATE COMMENT '当天日期',
    `back_ct`  BIGINT REPLACE COMMENT '回流用户数',
    `uu_ct`    BIGINT REPLACE COMMENT '独立用户数'
) engine = olap aggregate key (`stt`,`edt`,`cur_date`)
partition by range(`cur_date`)()
distributed by hash(`stt`) buckets 10
properties (
"replication_num" = "3",
"dynamic_partition.enable" = "true",
"dynamic_partition.time_unit" = "DAY",
"dynamic_partition.end" = "3",
"dynamic_partition.prefix" = "par",
"dynamic_partition.buckets" = "10"
);

-- 12.5 用户域用户注册各窗口汇总表（练习）
drop table if exists gmall2026_realtime.dws_user_user_register_window;
create table if not exists gmall2026_realtime.dws_user_user_register_window
(
    `stt`         DATETIME COMMENT '窗口起始时间',
    `edt`         DATETIME COMMENT '窗口结束时间',
    `cur_date`    DATE COMMENT '当天日期',
    `register_ct` BIGINT REPLACE COMMENT '注册用户数'
) engine = olap aggregate key (`stt`,`edt`,`cur_date`)
partition by range(`cur_date`)()
distributed by hash(`stt`) buckets 10
properties (
"replication_num" = "3",
"dynamic_partition.enable" = "true",
"dynamic_partition.time_unit" = "DAY",
"dynamic_partition.end" = "3",
"dynamic_partition.prefix" = "par",
"dynamic_partition.buckets" = "10"
);

-- 12.6 交易域加购各窗口汇总表
drop table if exists gmall2026_realtime.dws_trade_cart_add_uu_window;
create table if not exists gmall2026_realtime.dws_trade_cart_add_uu_window
(
    `stt`            DATETIME COMMENT '窗口起始时间',
    `edt`            DATETIME COMMENT '窗口结束时间',
    `cur_date`       DATE COMMENT '当天日期',
    `cart_add_uu_ct` BIGINT REPLACE COMMENT '加购独立用户数'
) engine = olap aggregate key (`stt`, `edt`,`cur_date`)
partition by range(`cur_date`)()
distributed by hash(`stt`) buckets 10
properties (
"replication_num" = "3",
"dynamic_partition.enable" = "true",
"dynamic_partition.time_unit" = "DAY",
"dynamic_partition.end" = "3",
"dynamic_partition.prefix" = "par",
"dynamic_partition.buckets" = "10"
);

-- 12.7 交易域支付成功各窗口汇总表（练习）
drop table if exists gmall2026_realtime.dws_trade_payment_suc_window;
create table if not exists gmall2026_realtime.dws_trade_payment_suc_window
(
    `stt`                           DATETIME COMMENT '窗口起始时间',
    `edt`                           DATETIME COMMENT '窗口结束时间',
    `cur_date`                      DATE COMMENT '当天日期',
    `payment_suc_unique_user_count` BIGINT REPLACE COMMENT '支付成功独立用户数',
    `payment_suc_new_user_count`    BIGINT REPLACE COMMENT '支付成功新用户数'
) engine = olap aggregate key (`stt`, `edt`,`cur_date`)
partition by range(`cur_date`)()
distributed by hash(`stt`) buckets 10
properties (
"replication_num" = "3",
"dynamic_partition.enable" = "true",
"dynamic_partition.time_unit" = "DAY",
"dynamic_partition.end" = "3",
"dynamic_partition.prefix" = "par",
"dynamic_partition.buckets" = "10"
);

-- 12.8 交易域下单各窗口汇总表（练习）
drop table if exists gmall2026_realtime.dws_trade_order_window;
create table if not exists gmall2026_realtime.dws_trade_order_window
(
    `stt`                     DATETIME COMMENT '窗口起始时间',
    `edt`                     DATETIME COMMENT '窗口结束时间',
    `cur_date`                DATE COMMENT '当天日期',
    `order_unique_user_count` BIGINT REPLACE COMMENT '下单独立用户数',
    `order_new_user_count`    BIGINT REPLACE COMMENT '下单新用户数'
) engine = olap aggregate key (`stt`, `edt`,`cur_date`)
partition by range(`cur_date`)()
distributed by hash(`stt`) buckets 10
properties (
"replication_num" = "3",
"dynamic_partition.enable" = "true",
"dynamic_partition.time_unit" = "DAY",
"dynamic_partition.end" = "3",
"dynamic_partition.prefix" = "par",
"dynamic_partition.buckets" = "10"
);

-- 12.9 交易域SKU粒度下单各窗口汇总表
drop table if exists gmall2026_realtime.dws_trade_sku_order_window;
create table if not exists gmall2026_realtime.dws_trade_sku_order_window
(
    `stt`                    DATETIME COMMENT '窗口起始时间',
    `edt`                    DATETIME COMMENT '窗口结束时间',
    `cur_date`               DATE COMMENT '当天日期',
    `trademark_id`           SMALLINT COMMENT '品牌ID',
    `trademark_name`         CHAR(255) COMMENT '品牌名称',
    `category1_id`           SMALLINT COMMENT '一级品类ID',
    `category1_name`         CHAR(128) COMMENT '一级品类名称',
    `category2_id`           SMALLINT COMMENT '二级品类ID',
    `category2_name`         CHAR(128) COMMENT '二级品类名称',
    `category3_id`           SMALLINT COMMENT '三级品类ID',
    `category3_name`         CHAR(128) COMMENT '三级品类名称',
    `sku_id`                 INT COMMENT 'SKU_ID',
    `sku_name`               CHAR(255) COMMENT 'SKU名称',
    `spu_id`                 INT COMMENT 'SPU_ID',
    `spu_name`               CHAR(255) COMMENT 'SPU名称',
    `original_amount`        DECIMAL(16, 2) REPLACE COMMENT '原始金额',
    `activity_reduce_amount` DECIMAL(16, 2) REPLACE COMMENT '活动减免金额',
    `coupon_reduce_amount`   DECIMAL(16, 2) REPLACE COMMENT '优惠券减免金额',
    `order_amount`           DECIMAL(16, 2) REPLACE COMMENT '下单金额'
) engine = olap aggregate key (`stt`,`edt`,`cur_date`,`trademark_id`,`trademark_name`,`category1_id`,`category1_name`,`category2_id`,`category2_name`,`category3_id`,`category3_name`,`sku_id`,`sku_name`,`spu_id`,`spu_name`)
partition by range(`cur_date`)()
distributed by hash(`stt`) buckets 10
properties (
"replication_num" = "3",
"dynamic_partition.enable" = "true",
"dynamic_partition.time_unit" = "DAY",
"dynamic_partition.end" = "3",
"dynamic_partition.prefix" = "par",
"dynamic_partition.buckets" = "10"
);

-- 12.10 交易域省份粒度下单各窗口汇总表
drop table if exists gmall2026_realtime.dws_trade_province_order_window;
create table if not exists gmall2026_realtime.dws_trade_province_order_window
(
    `stt`           DATETIME COMMENT '窗口起始时间',
    `edt`           DATETIME COMMENT '窗口结束时间',
    `cur_date`      DATE COMMENT '当天日期',
    `province_id`   TINYINT COMMENT '省份ID',
    `province_name` CHAR(128) COMMENT '省份名称',
    `order_count`   BIGINT REPLACE COMMENT '累计下单次数',
    `order_amount`  DECIMAL(16, 2) REPLACE COMMENT '累计下单金额'
) engine = olap aggregate key (`stt`, `edt`,`cur_date`,`province_id`,`province_name`)
partition by range(`cur_date`)()
distributed by hash(`stt`) buckets 10
properties (
"replication_num" = "3",
"dynamic_partition.enable" = "true",
"dynamic_partition.time_unit" = "DAY",
"dynamic_partition.end" = "3",
"dynamic_partition.prefix" = "par",
"dynamic_partition.buckets" = "10"
);

-- 12.11 交易域品牌-品类-用户粒度退单各窗口汇总表（练习）
drop table if exists gmall2026_realtime.dws_trade_trademark_category_user_refund_window;
create table if not exists gmall2026_realtime.dws_trade_trademark_category_user_refund_window
(
    `stt`            DATETIME COMMENT '窗口起始时间',
    `edt`            DATETIME COMMENT '窗口结束时间',
    `cur_date`       DATE COMMENT '当天日期',
    `trademark_id`   SMALLINT COMMENT '品牌ID',
    `trademark_name` CHAR(256) COMMENT '品牌名称',
    `category1_id`   SMALLINT COMMENT '一级品类ID',
    `category1_name` CHAR(128) COMMENT '一级品类名称',
    `category2_id`   SMALLINT COMMENT '二级品类ID',
    `category2_name` CHAR(128) COMMENT '二级品类名称',
    `category3_id`   SMALLINT COMMENT '三级品类ID',
    `category3_name` CHAR(128) COMMENT '三级品类名称',
    `user_id`        INT COMMENT '用户ID',
    `refund_count`   INT REPLACE COMMENT '退单次数'
) engine = olap aggregate key (`stt`,`edt`,`cur_date`,`trademark_id`,`trademark_name`,`category1_id`,`category1_name`,`category2_id`,`category2_name`,`category3_id`,`category3_name`,`user_id`)
partition by range(`cur_date`)()
distributed by hash(`stt`) buckets 10
properties (
"replication_num" = "3",
"dynamic_partition.enable" = "true",
"dynamic_partition.time_unit" = "DAY",
"dynamic_partition.end" = "3",
"dynamic_partition.prefix" = "par",
"dynamic_partition.buckets" = "10"
);
```